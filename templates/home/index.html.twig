{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1>{{ title }}</h1>
        <p class="lead">
            Це тестовий блог на Symfony. Тут можна переглядати пости та керувати тегами.
        </p>

        <div class="mt-4">
            <a href="{{ path('post_index') }}" class="btn btn-primary me-2">Переглянути пости</a>
            <a href="{{ path('tag_index') }}" class="btn btn-secondary">Переглянути теги</a>
        </div>
    </div>
{% endblock %}
